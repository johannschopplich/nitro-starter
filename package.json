{
  "name": "nitro-starter",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "dev": "nitro dev",
    "build": "nitro build",
    "preview": "node .output/server/index.mjs",
    "test": "vitest",
    "test:run": "vitest --run",
    "test:types": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier \"**/*.{css,html,json,md,ts,tsx,yml}\" --write",
    "prepare": "simple-git-hooks && nitro prepare"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.8.1",
    "eslint": "^8.57.0",
    "lint-staged": "^15.2.2",
    "nitropack": "^2.9.3",
    "prettier": "^3.2.5",
    "simple-git-hooks": "^2.10.0",
    "typescript": "^5.4.2",
    "vitest": "^1.3.1",
    "zod": "^3.22.4"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "**/*.ts": "eslint --cache"
  }
}
